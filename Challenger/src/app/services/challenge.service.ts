import {challenge} from '../models/challenge.model';

export class challengeService
{

	fetchChallengeList(sortorder:string,entries:number, entrydate:Date)
	{
		//will fetch and return an array of challenge objects for display

	}



	updateChallenge({id,heading,text,date,image})
	{
		//calls the update function on the correct challenge object.
	}
}